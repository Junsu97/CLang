#include<stdio.h>

void main(){
	int *p1;
	int *p2;
	int tmp;
	printf("a값 입력 : ");
	scanf("%d", &p1);
	printf("b값 입력 : ");
	scanf("%d", &p2);
	
	printf("%d %d",*p1,*p2);
	tmp = *p1;
	*p1 = *p2;
	*p2 = tmp;

	printf("바뀐 값 a는 %d, b는 %d",*p1,*p2);
}
