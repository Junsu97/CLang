#include<stdio.h>

void gugu(int dan){
	int result;
	for(int i=1; i < 10; i++){
		result = dan * i;
		printf("%d X %d = %d",dan,i,result);
	}
}

void main(){
	int dan;
	printf("출력하고 싶은 단을 입력 : ");
	scanf("%d", &dan);

	gugu(dan);
}
