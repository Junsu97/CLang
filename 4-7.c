#include <stdio.h>

void main()
{
	int a = 100, b = -200;

	printf(" 상수의 AND 연산 : %d \n", a && b ); //AND연산
	printf(" 상수의 OR 연산 : %d \n", a || b ); //OR연산
	printf(" 상수의 NOT 연산 : %d \n",!a ); //NOT연산

}
